[project]
name = "ai-agents-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=2.45.0",
    "pytest>=9.1.1",
    "python-dotenv>=1.2.2",
]
